{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "formatcode":"prettier docs package.json 'src/**/*.{js,json,scss,hbs}' --write --single-quote --trailing-comma all",
    "beautiful": "lint-staged",
    "start": "nodemon src/index.js",
    "watch": "node-sass -w src/resources/scss -o src/public/css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "lint-staged": {
    "src/**/*.{js,json,scss,hbs}": "prettier docs package.json --write --single-quote --trailing-comma all"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-handlebars": "^7.1.2",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.4",
    "morgan": "^1.10.0",
    "node-sass": "^9.0.0",
    "nodemon": "^3.1.0",
    "prettier": "^3.2.5"
  }
}
